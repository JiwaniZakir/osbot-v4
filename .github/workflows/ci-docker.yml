name: CI Docker

on:
  pull_request:
    branches: [main, dev]
  push:
    branches: [main]

jobs:
  docker-test:
    name: Tests in Docker
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build test image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          load: true
          tags: osbot-v4:ci-test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run lint inside container
        run: |
          docker run --rm osbot-v4:ci-test \
            sh -c "pip install ruff mypy -q && \
                   python3 -m ruff check src/osbot --output-format=text && \
                   python3 -m ruff format --check src/osbot && \
                   python3 -m mypy src/osbot || true"

      - name: Run tests inside container
        run: |
          docker run --rm \
            -e PYTHONPATH=/opt/osbot/src \
            -v ${{ github.workspace }}/tests:/opt/osbot/tests:ro \
            -v ${{ github.workspace }}/pyproject.toml:/opt/osbot/pyproject.toml:ro \
            osbot-v4:ci-test \
            sh -c "pip install pytest pytest-asyncio pytest-cov -q && \
                   python3 -m pytest tests/ --cov=osbot --cov-report=term-missing --cov-fail-under=30 -q"

      - name: Container health check
        run: |
          docker run --rm osbot-v4:ci-test python3 /opt/osbot/deploy/health_check.py || true
